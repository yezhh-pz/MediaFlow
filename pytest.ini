[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers
markers =
    integration: requires external services or real files
    slow: tests that take more than 10s
